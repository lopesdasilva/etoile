package controllers;


import models.User;
import play.mvc.*;

@Security.Authenticated(Secured.class)
public class Profile extends Controller{
	 /**
     * Display the dashboard.
     */
    public static Result index() {
        return ok(
            home.render(User.find.byId(request().username())));
    }
}
